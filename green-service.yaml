apiVersion: v1
kind: Service
metadata:
  name: green-service
  labels:
    app: bg-app
    version: green
spec:
  type: NodePort
  selector:
    app: bg-app
    version: green
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30030